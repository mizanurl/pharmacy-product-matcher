class Product {
    constructor(title, manufacturer, source, source_id) {
        this.title = title;
        this.manufacturer = manufacturer;
        this.source = source;
        this.source_id = source_id;
    }
}

export default Product;